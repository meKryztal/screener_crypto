# Скопируй этот файл в .env:
#   cp .env.example .env
#
# Несколько кодов — через запятую.
# Каждый код привязывается к отдельной куке.
# Удалил код — все кто входил по нему сразу слетают.

ACCESS_CODES=замени-на-свой-код,второй-код,третий-код
